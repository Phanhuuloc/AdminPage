<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oa.adminpage">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".OAApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity
            android:name=".presenter.activity.WelcomeActivity"
            android:label="@string/app_name"
            >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".presenter.ExampleActivity"
            android:label="@string/app_name"
             />
        <activity
            android:name=".presenter.activity.ProviderActivity"
             />
        <activity
            android:name=".presenter.activity.DetailActivity"
            android:exported="true"
            android:label="@string/title_activity_detail"
             />
        <activity
            android:name=".presenter.activity.SignInActivity"
            android:label="@string/app_name"
            >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".presenter.activity.OrderActivity"
            android:label="@string/title_activity_oder"
            />
        <activity
            android:name=".presenter.activity.MainManagerActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".presenter.activity.BillDetailActivity"
            android:exported="true"
            android:label="@string/view_bill"
            >

        </activity>
        <activity
            android:name=".presenter.activity.LoginActivity"
            android:label="@string/admin_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presenter.activity.AdminManagerActivity"
            android:exported="true"
            android:label="@string/title_activity_admin_manager"
            >

        </activity>

        <activity android:name=".presenter.activity.IntroActivity"
            android:exported="true">

        </activity>

        <activity android:name=".presenter.activity.AddNewProductActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>